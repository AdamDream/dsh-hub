- 制作模型等视觉产物后，偏好助手主动截图并查看实际效果进行自检，而非仅凭文字描述或生成成功就判断交付质量。 Confidence: 0.65
- 子代理协作偏好使用协调者统一协调，并显式传递精确修复范围与停止条件；在文件边界互不重叠且有协调者管理时倾向并行写入开发，否则同一文件集合采用单一写入者，以避免覆盖和中间态。 Confidence: 0.85
- 分析报告、问题清单等交付物完成交付后，用户会主动要求复核（如「再复核一下」），期望对其中事实论断重新验证后再视为定稿，而非一次成稿即接受。 Confidence: 0.60
- 规划重做类工序（如重新标定、重新配置）前，先盘点并核实已有成果——尤其用户亲手完成并提供过的文件/数据——是否已生效可复用，而非默认从头重做；用户会凭对自己历史工作的记忆主动质疑重复劳动（如「那不是已经有磁标定了么」），且此类质疑经核实往往成立，须据实调整计划。 Confidence: 0.65
- 审查或问题清单类分析应穷尽所有同类问题实例，而非仅列举个别代表性例子；用户会主动追问清单遗漏的同类案例。 Confidence: 0.65
- 规划新功能或新能力时，先分派 subagent 联网调研目标项目社区（GitHub 仓库、issues、npm/插件生态）有无现成实现，拿到调研结论与缺口分析后，再决定采用现成方案（fork 后补缺口）还是自研，而非直接动手实现。 Confidence: 0.65
- 供用户观察的视频/媒体样片（如相机采集样片、演示录像）应存放到用户主目录的视频目录（~/视频/，即 /home/<用户>/视频/），而非 /tmp 等临时目录，便于用户用播放器直接打开观察。 Confidence: 0.65
- 工具调用附带 sandbox_permissions 提权时，justification 是必填项且须填非空说明，空缺会被拒绝（用户验证填上非空说明后提权调用成功）；当前会话已是 danger-full-access（最高权限）时则无需提权参数，直接执行命令或写文件即可（同级提权曾被拒，用户亲自验证并两次纠正）。 Confidence: 0.85
- 当子代理或工具调用失败（如无法写文件、资料缺失）时，先排查是否为其自身工具路径或参数问题；要求区分工具故障与业务结论，不虚构审计通过，也不把资料缺失当作业务验证结果。 Confidence: 0.70
- 遇到报错或异常行为时，偏好先追问“为什么”并获得基于源码定位的机制级根因解释与完整因果链（含为何在特定场景高发），而非仅要表面修复或绕过；只有诊断证明链路确实存在缺陷后才实施最小代码修复，禁止直接修补观测数字（如在 replay 末端手工乘 scale 或加 offset 让指标好看）。 Confidence: 0.80
- workflow 中由审计阶段负责把代码任务拆解成细粒度、精确、可逐条实现的交付单元（含文件/函数/改动内容/验收标准）；修订并执行阶段按审计意见修订并落地代码，不扩范围、不自行重新拆解，单元有歧义时上报审计阶段澄清而非自行拍板。 Confidence: 0.70
- 偏好将非平凡、跨多轮工作锚定到持久目标；恢复推进前重新读取目标及其 revision，仅在工作完成或确实受阻时更新状态。 Confidence: 0.92
- 将 workflow run 视为可牺牲的调度而非持久状态；偏好各阶段把方案、审计发现和执行证据落盘，以便中断后从文件系统续接；遇到持续卡点（如磁标定 progress 恒 0%）或要切换到别的会话继续工作时，会先暂停死磕，要求 agent 整理工作进展、工作目标与遇到的问题，并新开一份可直接粘贴到新会话的交接提示词文件（如 NEXT_SESSION_PROMPT.txt，含链路、进展、已裁决且不得重开的事项、任务优先级、Runbook、历史坑），内容须以 git 状态与落盘审计为事实依据而非 agent 记忆。 Confidence: 0.95
- 对非平凡的多步骤工作偏好两阶段子代理闭环（审计 → 修订执行复核一体），不再单列独立复核阶段，以避免复核缺失上下文导致无意义的重复搜索和耗时；主代理负责编排与裁决，审计阶段直接读真实代码产出修订方案与细粒度交付单元，审计判定无需修改或否决时直接返回主代理、不进入执行；系统提示词与 AGENTS.md 应同步维护该方法论。 Confidence: 0.85
- 偏好先产出具体方案（改哪些文件、目录结构、验收标准）并经审计批准后再写代码，而非先写代码。 Confidence: 0.90
- 偏好每一步都产出可验证、可演示的输出/工件。 Confidence: 0.80
- 偏好按规定顺序先读现有文档再动手（read-before-act）；接手跨会话任务时将已落盘的审计/调研产物列为开工必读。 Confidence: 0.83
- 不重开已决事项；推翻既有决定需要充分理由并明确记录。 Confidence: 0.82
- 实质性任务指令用中文下达。 Confidence: 0.80
- 对独立调研线或开发支线（如 UI/UX、手套同步链、3D 可视化、新硬件驱动板、主线上游更新调研）偏好多线并行推进、多子代理并行扇出，而非单个串行深挖。 Confidence: 0.82
- 要可直接复制到终端运行的测试/验证命令（Runbook 形式），而非仅文字描述；多行命令行尾 \\ 续行符后不得有空格；当浏览器/GUI 打不开、无法目视确认时，会主动要求覆盖从零拉起全链路的完整测试 Runbook（前置检查、分步命令与预期输出、故障排查与停栈），以便自己粘贴到终端执行验证；涉及须用户亲手执行的硬件/实地操作（如手套磁标定与 T1-B 重录实测）时，会直接要求把完整命令序列贴到对话中（先读落盘 Runbook 再贴、与文件逐字一致），而非仅给文件路径让用户自己去翻；用户逐段执行后会把终端原始输出（含报错与异常，如设备节点缺失）整段贴回对话请求逐行判读，而非自行先排查。 Confidence: 0.92
- 偏好文档精简重组：合并并裁剪冗余内容。 Confidence: 0.70
- 偏好 taste GUI 用中文显示条目，底层 markdown 数据文件保持英文以保住提示词精度。 Confidence: 0.90
- 偏好置信度按两位小数精度显示（如 0.88），而非一位小数或整数百分比。 Confidence: 0.90
- 派发实质性工作前偏好先通过问答与用户对齐需求，而非假设范围；会主动以 /grill-me 方式要求 agent 把当前前沿决策问题一次性问完并附推荐方案；问答交互须使用专门的问答工具进行，而非在普通回复文本中罗列问题（用户曾明确纠正，此后每个新任务前均反复主动发起）。 Confidence: 0.85
- 偏好调研上游原始实现（如原始 GitHub 仓库、厂商官方 SDK 示例与文档）来分析并验证机制（含厂商工具能力缺口，如官方 Studio 不记录标定数据），而非仅依赖本地移植版本、既有结论或凭假设推断。 Confidence: 0.75
- 评估一个项目的能力能否整合进另一个项目时，偏好对上游实现与目标架构做深度源码级阅读，再显式映射可复用组件、框架专属适配层、生命周期钩子、存储与风险。 Confidence: 0.85
- 偏好派发实现前，先用最小端到端冒烟独立预检拟议 API 与集成假设，并把被证伪的假设记录为执行阶段的具体修正。 Confidence: 0.92
- 当审计或预检发现真实实现缺陷时，偏好把发现保留在证据基线中，并把修正后的 API 或流程显式注入执行简报，而非悄悄改动方案。 Confidence: 0.90
- 偏好 workflow 各阶段对 agent 空返回或异常这类瞬时失败最多用新 label 重试一次，并在落盘报告中保留首次失败原因。 Confidence: 0.90
- 偏好 workflow 编排把瞬时调用失败与 needs-revision、返工等业务裁决区分开，避免对有效的否定结论盲目重试。 Confidence: 0.90
- 偏好 workflow 最终结果递归 JSON 安全：可选字段带显式 fallback，不允许 undefined、Error、函数或 agent 对象穿越序列化边界。 Confidence: 0.90
- workflow 失败后，偏好先检查已落盘的阶段产物、git diff 和测试结果，再续接、重试或回滚；不因后续复核调用失败，就撤销已通过本地验证的实现。 Confidence: 0.90
- workflow 模型路由为单档：两阶段（审计、修订执行复核一体）与 taste 条目路由统一使用主力模型（当前 `adam/deepseek-v4-flash`）；subagent/subagent_fork 子代理由 preset 固定为与 workflow 同档（deepseek-v4-flash，adam 网关）；模型路由调整一律由用户明确指令裁决、agent 不擅自改动或换回旧模型（已历经 glm-5.3 → gpt-6-astra → deepseek-v4-flash 多轮切换，轻量 flash 模型也可作 workflow 执行档）；规划派生会话等新 agent 形态默认内置模型与主会话一致；btw 侧边对话默认模型不跟随主会话（独立默认，当前 deepseek-v4-flash）；每次 agent 调用仍需显式指定 provider 与 model，避免依赖默认。 Confidence: 0.75
- 当长对话漂移导致主代理凭记忆输出错误方案或幻觉被用户点名时，用户要求停止辩解，把全部方案重新分配给干净上下文的子代理重新读真实代码/文件审计，用事实裁决，而非主代理凭记忆增量修补原方案；用户已两次点名漂移并把该教训写入任务提示词：长对话中凭记忆给出的方案必须先派干净上下文的子代理重读代码验证。 Confidence: 0.80
- 分派 subagent 做调研类工作（联网调研、源码审计、资料检索、缺口分析）时，对关键结论交叉验证：至少并行派两个独立 subagent，或用第二个独立 subagent 复核第一个的结论，以事实裁决后再进入方案/执行，避免单一 subagent 的幻觉或遗漏直接污染裁决。 Confidence: 0.85
- 后续所有子代理均使用干净上下文，不通过分叉继承主会话上下文；以明确的任务边界和细化的边界要求替代大段上下文背景。 Confidence: 0.65
- 独立调研或开发支线派发子代理时，保持高并发并行扇出；不因子代理多而卡顿就主动减少并发，卡顿根源应以子代理独立线程与非流式输出等架构手段解决，而非靠降并发缓解。 Confidence: 0.80
- 处理非平凡项目卡点时，用户偏好把诊断实验设计成带完整观测量、边界检查和安全超时的可复现实验；磁标定实验应覆盖 Start→动作采集→End→结果轮询，并记录返回值、完成状态、失败节点、标定文件快照及前后传感器状态，而不是重复旧流程。 Confidence: 0.80
- 磁标定支线失败不应阻塞主线：应转入桥 JSONL 接线、现场重录、离线 analyze 与质量判定，磁标定保留为独立诊断支线。 Confidence: 0.75
- mHand 遥操改动偏好按三阶段边界提交：先桥修复及 Runbook/验收标准，再 Python live 桥接及测试，最后硬件重录与回填产物；厂商专属原始资源是否入 Git 遵循既有资产边界，不自动全量入仓。 Confidence: 0.75
- 对 mHand 数据质量验收不把所有 sensorState==1 作为硬门槛；应排除 SS_BadMag，并结合四元数连续性与 analyze 轴估计质量判断，结构性 SS_NONE 节点不单独判失败。 Confidence: 0.75
- 用户偏好在复杂任务开始时先设定持久化 goal，并在单轮会话中尽可能并行调研多个问题点，形成汇总后再集中进行问答对齐；应优先推动实际文件/报告落地，避免无意义的多轮问答而没有可验证产物。 Confidence: 0.75
- 升级或替换上游软件时，偏好先基于真实运行盘面、上游版本和本地定制做事实审计，穷举受影响的补丁、依赖与数据兼容性风险；避免原地覆盖，先完整备份并创建并行新 profile，完成配置、插件、会话数据和端到端验证后再切换，以保留可回滚路径。 Confidence: 0.85
- 子代理只应在发生真实写入冲突、任务越界或用户明确要求停止时被 interrupt；单一 coordinator/writer 或只读审计代理应让其自然完成，避免频繁打断造成任务震荡。 Confidence: 0.75
- 用户偏好在制作具有明确公众形象的角色模型时，先查阅网络上的可靠图片或资料确认标志性外观，再开始建模；不接受仅凭一般印象制作的相似但错误的替代品。 Confidence: 0.85
- 动手/手工操作前，要求讲清底层语义——每一步在测什么/估什么、测量量如何向下游传播（如标定中性位是否被重映射到下游模型的中性位、失配的影响）、预期可观察的结果（如关节角变化）——以便精确执行，而非照抄命令；请求完整操作流程（如标定流程）时会明确要求附带语义与关节角说明；当流程信息在多轮对话中碎片化积累、用户感到混乱时，会明确要求抛弃前文累积（「你把前面的东西都忘掉」）、从零重述完整系统性的流程总览——各层流程加每层负责的关节角名称与跨层叫法对照，而非增量式补充或依赖对话记忆。 Confidence: 0.85

--- /home/CNS2026495165/.dsh/profiles-archive/web2-20260915-105429/node_modules/@deepseek-ai/dsh-subagent-fork-in-process/lib/index.js	2026-09-11 14:27:15.068962229 +0800
+++ /home/CNS2026495165/.npm-global/lib/node_modules/@deepseek-ai/dsh/node_modules/@deepseek-ai/dsh-subagent-fork-in-process/lib/index.js	2026-09-12 14:56:15.642706719 +0800
@@ -21,21 +21,19 @@
 * @returns the seed events, contiguous from seq 0; empty when no turn has completed.
 */
 function completedTurnPrefix(parent) {
-	const events = parent.session.snapshotEvents();
+	const events = parent.session.events;
 	const lastEnd = events.findLast((e) => e.type === "turn/end");
 	if (lastEnd === void 0) return [];
 	return events.slice(0, lastEnd.seq + 1);
 }
 /**
 * The fork provider. Supports `depthLimit` and `outputSchema` (via the shared
-* in-process structured runtime), `agentOptions` (merged over the parent
-* route), and `toolFilter`/`persona` (scoped restrict() and a scoped shadowing
-* persona section).
+* in-process structured runtime), plus `toolFilter`/`persona` (scoped
+* restrict() and a scoped shadowing persona section).
 */
 var ForkInProcessProvider = class {
 	name;
 	capabilities = {
-		agentOptions: true,
 		outputSchema: true,
 		depthLimit: true,
 		toolFilter: true,

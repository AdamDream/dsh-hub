--- settings.yaml
+++ settings.yaml
@@ -77,6 +77,7 @@
       models:
         - id: deepseek-v4-flash
+          contextWindow: 1000000
-          maxTokens: 786432
+          maxTokens: 990000
@@ -135,4 +135,7 @@
 vision-adam:
-  model: glm-5.3-flash
-  maxTokens: 100000
+  model: deepseek-v4.1-flash
+  baseURL: https://opencode.ai/zen/go/v1
+  apiKeyEnv: OPENCODE_GO_API_KEY
+  maxTokens: 393216

# ============================================================================
# 注意（2026-09-12 事故教训，勿回退）：
# 绝不向 llm-pi-ai 的 opencode-go 路由添加 catalog 未描述的模型条目
# （如 deepseek-v4.1-flash）。该路由的 catalog 模型 api 不一致
# （anthropic-messages / openai-completions / openai-responses 三种），
# sharedCatalogApi() 无解 → 新条目解析不出 api/baseUrl → resolveRouteModels
# 抛 invalid(provider) → 整段 llm-pi-ai 不可服务 → adam 与 opencode 模型
# 全部从 web 模型选择器消失（2026-09-12 实测事故，已修复）。
#
# opencode deepseek-v4.1-flash（识图模型）的 maxTokens 取网关实测硬上限
# 393216（2026-09-12 实测：opencode 网关 max_tokens 有效范围 [1, 393216]，
# 990000 会被拒绝；adam 网关实测接受 990000）。vision-adam lib 直连网关，
# max_tokens 取该段 maxTokens，无需也不得在 pi-ai providers 中登记该模型。
#
# 诊断脚本：.workspace/diag-piai-route.mjs（复刻 resolveRouteModels 解析，
# 可对任意 settings 快照做可服务性预检）。
# ============================================================================

# 前后对比复测数据（验收用）

**基线来源**：`.workspace/settings-lag/DIAGNOSIS.md` §1.2（同一套方法：rAF 帧间隔 + CDP Performance + WS 帧率）
**判读纪律**（C1 探针内置）：**必须先看 `ws_rate_per_s` 是否与基线 73 帧/s 同量级**，否则不能与基线数字直接比。

---

## 一、客户端侧（已生效，无需重启）

| 窗口 | 条件 | script ms/s | 帧 p50 | 帧 p99 | 帧 max | >50ms 帧 | ws 帧/s |
|---|---|---|---|---|---|---|---|
| **基线**（未打补丁，N=2396） | 活跃流式 | **121** | 16.7 | **100.1** | 133.4 | **22** | 73 |
| 打补丁后，N=2396 | 活跃流式 | 45.0 | 16.7 | 33.3 | 83.4 | 9 | 92.5 |
| 打补丁后，N=734 | 安静 | 16.4 | 16.7 | 16.8 | 50.1 | 1 | 5.3 |
| **打补丁后，N=734** | **活跃流式（ws 135，比基线更重）** | **27.5** | 16.7 | **33.3** | 116.7 | **4** | **135.2** |

**可比条件下的结论（第 1 行 vs 第 4 行）**：

| 指标 | 基线 | 现在 | 变化 | 验收门槛 | 判定 |
|---|---|---|---|---|---|
| 空闲主线程脚本 | 121 ms/s | **27.5 ms/s** | **−77%** | < 60 ms/s | ✅ 通过 |
| 帧 p99 | 100.1 ms | **33.3 ms** | **−67%** | — | ✅ |
| >50ms 卡顿帧 | 22 | **4** | **−82%** | 降 >50% | ✅ 通过 |

设置页窗口（同一探针第 2 段）：

| 窗口 | 条件 | script ms/s | 帧 p99 | >50ms 帧 | ws 帧/s |
|---|---|---|---|---|---|
| 基线·设置打开 | 活跃 | 2550ms/20s | 116.6 | 26 | — |
| 基线·设置停留 | 活跃 | 190 ms/s | 116.7 | 40 | — |
| 现在·设置打开 | ws 41.6 | 29.6 ms/s | **49.9** | 7 | 41.6 |
| 现在·设置停留 | ws 1.7（流式已停，**不可比**） | 17.3 ms/s | 16.8 | 4 | 1.7 |

→ 设置页打开窗口 **p99 49.9ms < 50ms**，达到门槛；停留窗口因流式已停（ws 1.7）**不与基线直接比**。

---

## 二、会话列表规模（数据清理已生效）

| 项 | 基线 | 现在（phase 1 后） | 说明 |
|---|---|---|---|
| `session.list` 条目 | 2,396 | **734**（顶层 87 + subagent 647） | −69% |
| `session.list` 字节（items） | 3,941,050 | **1,225,755** | **−69%** |
| subagent 行数 | 2,309 | 647 | 已删 1,670 个 7 天前 subagent 会话 |
| 顶层会话 | 87 | **87（一条不少）** | ✅ 探针 A3a/A3b PASS |
| 磁盘 `sessions` 体积 | 1.2 GB | **473 MB** | −61% |

**待重启生效**（服务端过滤 + 聚合字段）：条目 → **≤287**、字节 → **≤500 KB**、顶层行带 `runningSubagentCount`。

---

## 三、宿主侧（**待重启**，当前仍是旧代码在内存里）

| 端点 | 基线（重启前实测） | 目标 | 现状 |
|---|---|---|---|
| `/usage/heatmap` | 285~306 ms | < 40 ms | 待重启（补丁已落盘，A 档沙箱实测 289ms→**0.081ms**） |
| 宿主事件循环停顿 | 264.9~272.2 ms | 无 >100ms 停顿 | 待重启 |
| 宿主延迟中位数 | 2.1 ms | < 5 ms | ✅ 基线即达标 |

---

## 四、当前未达标项与原因

| 项 | 现状 | 原因 |
|---|---|---|
| `session.list` ≤287 条 / ≤500KB | 734 条 / 1.23MB | **服务端过滤需重启**（补丁已在盘上） |
| `runningSubagentCount` 字段 | 0/87 | 同上 |
| `/usage/heatmap` <40ms | 未测（需重启后测） | 用量插件**宿主半**需重启 |

**结论**：客户端侧三项验收门槛**全部通过**（−77% / −67% / −82%）；剩余三项等一次宿主重启。

---

## 五、原始证据文件

| 文件 | 内容 |
|---|---|
| `.workspace/settings-lag/measure.json`、`measure3.json`、`measure5.json` | 基线（帧/脚本/慢请求/WS 速率/session.list 规模） |
| `.workspace/lag-fix/reports/measure-after-C1-v2.json` | 打补丁后 N=2396 |
| `.workspace/lag-fix/reports/measure-after-N734.json` | 数据清理后 N=734（安静条件） |
| `.workspace/lag-fix/reports/measure-after-N734-active.json` | **数据清理后 N=734（活跃流式 ws 135，主对比数据）** |
| `.workspace/lag-fix/reports/probe-post-codeapply.json` | 服务端全量态（重启前） |

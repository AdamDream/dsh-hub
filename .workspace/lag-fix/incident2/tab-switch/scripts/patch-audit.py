import io
p = '/home/CNS2026495165/dsh/.workspace/lag-fix/incident2/tab-switch/audit.md'
s = io.open(p, encoding='utf-8').read()

# ---- §3.5 rewrite + add §3.6 ----
start = s.index('### 3.5 「插件」栏目内部子标签')
end = s.index('---\n\n## 4. 滚动（任务 3）')
new = u'''### 3.5 「插件」栏目内部子标签（`raw/tabswitch-run5.json`，每次点击前重读坐标）

| 点击目标 | `aria-selected` 是否切换 | dlg 节点 | 内容节点 | dlg SVG | 本窗 RPC |
|---|---|---|---|---|---|
| （初始）| 插件配置 = 选中 | 425 | 341 | 14 | — |
| r1 → **插件列表** | 是 | **174**（列表尚在加载）| 90 | 12 | **`/api/pluginInventory/list` ×1** |
| r1 → 插件配置 | 是 | 425 | 341 | 14 | 0 |
| r2 → **插件列表** | 是 | **1820** | **1736** | **192** | 0（数据已到）|
| r2 → 插件配置 | 是 | 1820 | 1736 | 192 | 0 （见下）|
| r3 → 插件列表 | 是 | 1820 | 1736 | 192 | 0 |
| r3 → 插件配置 | 是 | 1820 | 1736 | 192 | 0 （见下）|

**可确定的结论**
- `插件列表`（`all`）挂载 → **恰好 1 次 `/api/pluginInventory/list`**；该请求在 1.8 s 窗口结束时**仍未 settle**（`ms` 缺失）⇒ **耗时 > 1.8 s**（终值未取到，标 INCONCLUSIVE）。静态侧证据（`raw/sub-section-cost.md`）：宿主实现每次调用直读 Loader、**无缓存**。
- **`插件列表` 完整渲染规模 = 1820 dlg 节点 / 1736 内容节点 / 192 SVG**，对比 `插件配置` 的 425 / 341 / 14 ⇒ **4.3× / 5.1× / 13.7×**。这是本设置页里 DOM 规模最大的面板。
- 全部 6 个窗口：**0 长任务**、`ΔLayout` 1–2、`ΔRecalc` 6–7、`ΔScript` 0.010–0.019 s（与空操作同量级）。

**未能分离的部分（INCONCLUSIVE）**
r2/r3 的「→ 插件配置」点击后 `aria-selected` 已切回「插件配置」，但**内容规模仍为 1820 节点、未回落到 425**，且本窗 `ΔScript` 与空操作同量级 ⇒ 要么该子标签切换存在**状态与渲染不一致**（疑似前一子标签内容未卸载），要么本线器械未能可靠驱动它。**本线无法区分这两种解释**，故子标签的「切换成本」标 INCONCLUSIVE；上面两条「可确定结论」不受影响。

### 3.6 静置（dwell）12 s —— 最大的面板闲着的时候完全空闲

在 **1820 节点 / 192 SVG** 的 `插件列表` 面板静置 12 s（`raw/tabswitch-run5.json → dwell`，并发门禁通过）：

| 指标 | 值 |
|---|---|
| 帧 | 719 帧，p50 **16.7** / p95 **16.8** / p99 **16.8** / max **16.9** ms，>50ms = **0** |
| 长任务 | **0**（0 ms）|
| RPC | **0**（SSE 流亦 0）|
| `ΔScriptDuration` | **0.0375 s / 12 s ≈ 3 ms/s** |
| `ΔRecalcStyleCount` / `ΔLayoutCount` / `ΔLayoutDuration` | **0 / 0 / 0** |
| DOM churn | added / removed / attributes / records 全 **0** |
| CDP `Nodes` / `JSEventListeners` | **0 / 0**（无泄漏、无增长）|

⇒ **本页最重的面板在静置时零活动。** 这一条**撤回**了 run4 中「1820 节点面板存在 ≈70 次/s 持续样式重算」的观察（该观察属瞬态/受并发污染，run5 干净复测为 0，详见 §7 #4）。

'''
s = s[:start] + new + s[end:]

# ---- §4.5 大列表重绘行 ----
s = s.replace(
 u'| 来自**大列表重绘** | **INCONCLUSIVE** | 帧成本与列表规模不成比例（851 px 的插件与 74 px 的通用设置帧 p95 同为 ~19–23 ms）；但 1820 节点面板有持续 ΔRecalc ≈70/s 未定位 |',
 u'| 来自**大列表重绘** | **FAIL（否定）** | ① 帧成本与列表规模不成比例（851 px 的插件与 74 px 的通用设置帧 p95 同为 ~19–23 ms）；② 12 s 静置下 1820 节点 / 192 SVG 的 `插件列表` 面板 `ΔRecalc=0 / ΔLayout=0 / churn=0`（§3.6）⇒ **该面板不产生持续重算/重绘** |')

# ---- §6 候选 3 ----
s = s.replace(
 u'- **预期收益**：1830 节点 / 192 SVG 的面板 **是通用设置的 10.8× 节点**，且该面板观察到持续 ΔRecalc ≈70/s。虚拟化 + 缓存可同时降低挂载尖峰与常驻重算。',
 u'- **预期收益**：`插件列表` 完整渲染 **1820 dlg 节点 / 1736 内容节点 / 192 SVG**，是通用设置的 **4.3× 节点 / 13.7× SVG**，且每次挂载直读 Loader 的 `/api/pluginInventory/list` **耗时 > 1.8 s**。虚拟化 + 缓存可降低挂载尖峰与无缓存重扫。（「常驻重算」这一预期收益已被 §3.6 实测排除。）')
s = s.replace(
 u'- **验收标准**：`插件列表` 挂载后 `dialogNodes` 从 **1820 降到 ≤400**；连续切 3 轮 `pluginInventory.list` 调用数 ≤1；静置 12s 的 `ΔRecalcStyleCount` 从 ~70/s 降到 ~0。',
 u'- **验收标准**：`插件列表` 挂载后 `dialogNodes` 从 **1820 降到 ≤400**、`dlgSvg` 从 192 降到 ≤40；同一会话内 `pluginInventory.list` 调用数 ≤1（现在每次挂载 1 次）；并顺带确认 `插件列表 ↔ 插件配置` 互切时**旧子标签内容确实被卸载**（§3.5 的 INCONCLUSIVE 项）。')

# ---- §7 #4 撤回 ----
s = s.replace(
 u'| 4 | **`插件列表` 面板上 ~70 次/s 的持续样式重算** | **INCONCLUSIVE（未定位）** | run4 在 1820 节点面板下静置与滚动均报 ΔRecalc≈154/2.2s；run1 的 421 节点状态下为 0。**未定位到具体元素/动画**（需 CDP `StyleRecalcInvalidationTracking` 或 trace StyleRecalc 归因）。 |',
 u'| 4 | ~~`插件列表` 面板持续样式重算 ≈70/s~~ | **已撤回** | run4 曾在 1820 节点面板下观测到 ΔRecalc≈154/2.2s；**run5 在同等条件下静置 12 s 复测为 `ΔRecalcStyleCount = 0`**（§3.6）⇒ 该观察**不成立**，判为瞬态/受并发污染。**原结论撤回，不作为任何修复依据。** |')

# ---- §7 #5 ----
s = s.replace(
 u'| 5 | **`插件列表` 与 `插件配置` 的干净 A/B 时间量** | **部分缺失** | run4 的子标签点击**坐标失效**（首击后按钮位移，后续点击落回同一标签），run3 的修复版因脚本 `ReferenceError` 中断；run5 为修坐标复测（结果见 §3.5 注），但"子标签互切是否重发 RPC"仍只在部分窗口确定。 |',
 u'| 5 | **子标签「插件配置」互切的真实行为** | **INCONCLUSIVE** | 修坐标后（run5）`aria-selected` 每次都切换，但 r2/r3 的「→插件配置」后内容仍为 1820 节点、未回落 425（§3.5）。**「状态与渲染不一致」与「器械未能驱动」两种解释无法区分**；需在真实 Chrome 里人工点击确认。 |')

s = s.replace(u'run1 48 + run2 14 + run4 13 = **75 个带器械的窗口**里',
              u'run1 48 + run2 14 + run4 13 + run5 6 = **81 个带器械的窗口**里')

# ---- §0 表格里 4① 补一句 ----
s = s.replace(
 u'| 4① | 哪些标签**确实**更贵（同窗倍数、"是否可测差异"） | **PASS** | 「插件」= **9 个 RPC/切换（全部 `/usage/*`，77–791 ms）**、内容子树 337 节点；「插件列表」子标签 = 1820 节点/192 SVG。其余 0–4 RPC。RPC 数是**完全可分离**的判据。见 §5.1 |',
 u'| 4① | 哪些标签**确实**更贵（同窗倍数、"是否可测差异"） | **PASS** | 「插件」= **9 个 RPC/切换（全部 `/usage/*`，77–791 ms）**、内容子树 337 节点；「插件列表」子标签 = **1820 节点 / 192 SVG**。其余 0–4 RPC。**RPC 数完全可分离**；时间量**不可分辨**。见 §3、§5.1 |')

io.open(p, 'w', encoding='utf-8').write(s)
print('patch B applied')

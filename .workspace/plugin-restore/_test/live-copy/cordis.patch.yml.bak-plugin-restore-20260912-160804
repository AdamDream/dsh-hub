# Your patch layer for this dsh profile, applied after every bundle layer:
# a top-level YAML array of loader patch entries (id-targeted config
# overrides, disables, and insert lists; `!!js` expressions allowed).
- insert:
    - id: vision-adam
      name: '@deepseek-ai/dsh-vision-adam'

# 默认 agent preset 换成用户自建的 standard-glm（$DSH_HOME/.agent-presets/）：
# 与官方 standard 唯一区别是 subagent / subagent_fork 的 agentOptions 固定
# provider=adam、model=deepseek-v4-flash。组合变更需重启 DSH 后对新会话生效。
- id: agent-presets
  config:
    default: standard-glm
- insert:
    - id: taste
      name: '@deepseek-ai/dsh-taste'
- insert:
    - id: btw
      name: '@local/dsh-btw'

# 壁纸插件（@frog755/dsh-wallpaper 的本地 fork，静态图片版）：
# per-page 覆盖 + 暗色遮罩 + URL/路径/上传来源，配置走 ~/.dsh/settings.yaml。
- insert:
    - id: wallpaper
      name: '@local/dsh-wallpaper'

# dsh-fix: disabled entry "wallpaper" at 2026-09-12T06:31:35.260Z
- id: "wallpaper"
  disabled: true

# dsh-fix: disabled entry "vision-adam" at 2026-09-12T06:32:30.396Z
- id: "vision-adam"
  disabled: true

# dsh-fix: disabled entry "subagent-model-selection-settings" at 2026-09-12T06:32:30.980Z
- id: "subagent-model-selection-settings"
  disabled: true

# dsh-fix: disabled entry "taste" at 2026-09-12T06:44:42.256Z
- id: "taste"
  disabled: true

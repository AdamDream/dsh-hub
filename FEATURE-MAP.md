# 功能地图 —— dsh-hub 能力状态与起点

> **Tier 2 · 能力** · [指南地图](README.md)

先读本页，再决定用哪个能力、去哪份文档。每一项都给出**状态（带日期）**、
**入口**与**一句话说明**；状态词汇与[README](README.md)一致，可复判。

| 状态标记 | 含义 |
| --- | --- |
| **已实现·实测** | 已落地部署，并在本机实测/验收过（GUI 验收矩阵或真实调用）。 |
| **已实现·未真机** | 代码与本地单测通过，但依赖真机环境的部分未实测（在说明中标注哪部分）。 |
| **尚不可用** | 机制存在或闸门通过，但当前不可用（具名原因）。 |
| **计划中** | 当前未实现，计划后续提供。 |
| **延期** | 长期方向；不要围绕它设计。 |

> 数据时点：2026-09-16。事实来源：`.workspace/*-exec.md` / `*-audit.md` 落盘报告（每项入口列给出）；
> 本页不臆造状态，条件性与未开放项一律如实标注。部署基线：DSH 0.1.1-rc.2（profile web）。

---

## 一、插件（`@local/*` 与 `@deepseek-ai/*`，部署于 `~/.dsh/profiles/node_modules/`）

| 能力 | 状态 | 入口 | 一句话说明 |
| --- | --- | --- | --- |
| **dsh-btw 侧边对话 v2** | 已实现·实测（2026-09-12 部署；09-16 能力检测 + 行为开关；09-17 默认模型换 `deepseek-v4.1-flash` + settings 热读；**09-23 问答卡片持久化修复 + 选项行对齐主会话，已部署并真机验收（served `88de97e6c22fc6de9ebd61cb27e5779f` / `?rev=3980d1322992`）；09-23 D30 修复（读失败可见化已热生效；宿主面工具边界 codec 自校验已部署、**待重启生效**，served `66beb3455c59f4991355f3918228e495` / `?rev=887a12106dcd`）**） | [btw-v2-runbook.md](.workspace/reports/runbooks/btw-v2-runbook.md) · [vision-settings-capability-exec.md](.workspace/reports/execs/vision/vision-settings-capability-exec.md) · [p0b-settings-switch-exec.md](.workspace/reports/execs/p0-hotload/p0b-settings-switch-exec.md) · **`.workspace/btw-question/exec-report.md`** | 侧聊插件：粘贴图片经 vision-adam 转文本 + R1-9 模板包装、转录缩略图可放大；子代理树/项目总览跳转列表（listTree/listProject）；面板对齐（运行横幅、工具行、图片序号徽标）；模型声明支持图片时原图直传。2026-09-17：侧聊默认模型 → `deepseek-v4.1-flash`（旧持久值 `deepseek-v4-flash` 经 legacy 映射回落，不崩不白屏）；默认/清单改由 `dsh-btw.model.default` / `model.options` **每次调用热读**（缺失回退常量默认）。**2026-09-23（问答卡片）**：修掉「点选后选中态 ~220 ms 被抹掉」（根因＝重置 effect 依赖每次 `sideChat/read` 都换身份的 `pendingQuestion.questions`；改官方同款「删除重置 effect + 调用点 `key={questionId}` 重挂」，官方卡片本身零 `useEffect`）；选项行语义/观感向主会话对齐（`role=radio\|checkbox` + `aria-checked` 取代非法的 `aria-pressed`、单选项序号徽标 / 多选复选框、去掉硬编码绿 `#b7e85b`、选中底/边改用官方实测 token），**`.questionCard` 绿壳按用户裁决保留**；测试面从零覆盖补到 C1–C5 回归锁（全量 25 files / 250 passed / 2 skipped，D29 批次计数）。**部署与真机验收（2026-09-23 完成）**：`cp -a dsh-btw/lib/client.js ~/.dsh/profiles/node_modules/@local/dsh-btw/lib/client.js`（热面，无需重启 dsh）——线上现服务 `66beb3455c59f4991355f3918228e495` / 365 269 B / `?rev=887a12106dcd`（问答卡片终版 `88de97e6…`、更早 `241b04c4…`、`6c29b98b645d`）。**真机 E2E**（headless Chromium 驱动真实抽屉 + 真实 `btw_ask_user`，50 ms×3 s 采样）：修复前 `REVERTED 166→270 ms（2/56）` ⇒ 问答卡片终版 **`PERSISTED 185 ms 起全程（55/55）`**。观感增强（用户裁决）：选中时序号徽标翻 **btw 绿实心 + 深色数字**（仅靠官方选中底实测全行均差 13.01/255，易读成"没选上"），并把选项**基态文字改主色 `rgb(15,17,21)`** 补齐最后一处官方偏差。**2026-09-23（D30）**：工具侧参数 schema 是开放记录（`additionalProperties: true`）而下发走 strict codec ⇒ 模型多带一个未声明键（如驼峰 `multiSelect`）会让该 btw 的 `sideChat/read` **整体**失败，抽屉**冻结在最后一次成功快照**（跑马灯恒停「输出中… · 当前动作: btw_ask_user」、无卡片、无报错、无消息更新；真机 27 次失败读 / 45 s、warn 间隔中位 1.475 s），子代理阻塞，**唯一出路是按「停止」**（「收起→重开抽屉」无效）。**修法**：`btw_ask_user.execute` 内用**与下发同一 schema** 做 codec 自校验（fail-closed：失败即抛含 path/code + 合法键清单的可纠正错误、**不写 pending**）+ 选项项补 `.strict()`；客户端加**读失败可见化**（连续失败 ≥3 次给非阻塞提示「实时更新已暂停，正在重试」`drawer.readRetrying`，成功读自动清除、`phase` 仍为 `open`）。测试 **25 files / 260 passed / 2 skipped (262)**（新增 10 例：宿主 7 + 客户端 3）。**生效面分开记**：客户端面（`lib/client.js`）**热面已生效**；宿主面（`lib/index.js` = `e1437b3ba7de953811e65c47d5b392e5` / 67 542 B）**已部署但宿主未重启 ⇒ 未生效**。证据：`.workspace/btw-question/d30-consequence.md` · `.workspace/btw-question/d30/` · `.workspace/btw-question/exec-d30/report.md`。**2026-09-23（真机补测收口）**：D29 批遗留的 4 项「未覆盖」**已全部真机补测 PASS**（多选：不给键名即得 `role=group` + 3×`role=checkbox`，点选后 **59/59 采样 2 967 ms 保持**、提交后卡片 1 ms 内消失；「回答后再提问」重挂：草稿全清 + 题干/选项全变、重挂后 **59/59（2 974 ms）**；窄屏 640×800 `bottom-sheet` 选项行：**2 ms** 内切换、bbox 全在视口内、点选 **59/59（2 963 ms）**、未被 scrim 吞点击；键盘/焦点序：Space 切换 **59/59**、再按取消 **0/59**、Enter 可切换且未误提交、方向键无效，**官方真机对照逐项一致 ⇒ APG 偏差为上游同款**）；被测版本已取证（实际加载 `?rev=887a12106dcd` / md5 `66beb345…` / 365 269 B、4 轮 0 pageerror）。**同时登记 1 项已知缺陷（未修，用户裁决另开一轮）＝ notebook §7 D33**：窄屏 bottom-sheet 下抽屉内容溢出（640×800：`scrollHeight 633 > clientHeight 370`、transcript 滚动窗仅 54 px）⇒「停止」与输入框中心点在视口外（`elementFromPoint=null`），1440×900 正常；根因＝泳道高度沿用 `0.48×可用高度`（夹 280–560）而内容更高，且 `<720 px` 刻意不套用显式尺寸 ⇒ **非本批回归**。证据：`.workspace/btw-question/e2e-cover/report.md` + `raw-*-T1/T2/T3/T4.json`、`raw-*-T3-extra.json`、`t3-geometry-probe.mjs`。 |
| **dsh-usage 用量统计 + tooltip** | 已实现·实测（2026-09-14；2026-09-18 趋势图改造） | [usage-tooltip-exec.md](.workspace/reports/execs/usage/usage-tooltip-exec.md) · [usage-heatmap-exec.md](.workspace/reports/execs/usage/usage-heatmap-exec.md) · [usage-chart-restart-acceptance.md](.workspace/reports/execs/acceptance/usage-chart-restart-acceptance.md) | 面积/柱状/热力图（自绘 SVG）；三图自绘跟随鼠标 tooltip（日期 MM-DD + token 紧凑格式化，四象限防溢出）；热力图蓝阶/空心/月份标签/峰值环。**2026-09-18 改造**：面积图改 **3 小时合并的 monotone-cubic 平滑曲线**（浅蓝描边 + 深蓝半透明幅底，实测过冲 0.0000px），柱状图保持按日但改**按值线性黄→橙**，X 轴标签按桶键格式化为 `MM-DD` / `MM-DD HH`（修掉 `slice(5)` 对小时键的退化 bug）；数据侧 `timeseries` 新增 `granularity: "hour"`（默认仍 day，旧客户端零影响），客户端按窗口补 0 成连续桶。工具列：`.workspace/workstreams/sources/dsh-usage-src/dev/verify-trend-v2.mjs`（68 项自检）· `render-trend-v2.py`（真实几何 + PIL 离线预览）。 |
| **行为开关（settings 值级热载）** | 已实现·实测（2026-09-16） | [p0b-settings-switch-exec.md](.workspace/reports/execs/p0-hotload/p0b-settings-switch-exec.md) | dsh-usage 5 键（ui.tooltip / heatmap.peakRing / monthLabels / legendNote / levels）+ dsh-btw 4 键（ui.banner / modelSelect / imageBadge / vision.autoTransform）；改 `~/.dsh/settings.yaml` 即生效，默认值 = 旧行为。 |
| **vision-adam 识图（2026-09-17 起走 adam 网关）** | 已实现·实测（2026-09-12 配置化；09-14 提示词；09-17 换网关） | [vision-prompt-exec.md](.workspace/reports/execs/vision/vision-prompt-exec.md) · [mmt-probe/RESULTS.md](.workspace/probes/mmt/RESULTS.md) | 默认网关 = adam（`https://llmapi.roboscience.xyz/v1` + `ADAM_API_KEY`，model `deepseek-v4.1-flash`，maxTokens 393216 实测被接受）。**尾斜杠事实更正（2026-09-17 源码+运行态核实）**：真正会拼出 `//chat/completions` 的是本插件 `lib/index.js:161` 的手写 `` fetch(`${baseURL}/chat/completions`) ``（provider 层因 OpenAI SDK `openai/client.js:224` 会切前导斜杠而无症状），且那句 `Invalid URL (POST /v1//chat/completions)` 实为**网关 404 响应体文案**、失败工具是 `analyze_image`；现已加 `.replace(/\/+$/, "")` 归一化加固，不再依赖"配置恰好无尾斜杠"。；提示词为「完整转录 + 审美/设计合理性分析」（真实 API 冒烟通过）。opencode go 网关配置可作为回退手动改回。 |
| **vision-adam 识图设置页** | 已实现·实测（2026-09-16 部署） | [deploy-vision-settings/README.md](.workspace/workstreams/deploy/deploy-vision-settings/README.md) | settings.section「vision-adam 识图设置」（order 60）：model / baseURL / apiKeyEnv / maxTokens 字段级读写，清空即恢复默认；apiKey/maxBytes 等键永不触碰。 |
| **dsh-subagent-model 子代理模型设置页** | 已实现·**已重启验收**（2026-09-17 部署；重启 17:45:39 后实测：boot 图含该插件、bundle HTTP 200/17099B 含「子代理模型」+ `settings.section` + `order: 70`、host 侧命名空间注册原文已核；GUI 目视待用户） | [acceptance-exec.md](.workspace/reports/execs/acceptance/acceptance-exec.md) §2 P1-6 · [subagent-model-settings-exec.md](.workspace/reports/execs/subagent-model/subagent-model-settings-exec.md) · [deploy-subagent-model/README.md](.workspace/workstreams/deploy/deploy-subagent-model/README.md) | 新插件 `@local/dsh-subagent-model`：host 注册 `dsh-subagent` settings 命名空间（provider/model 可选）+ 浏览器侧 settings.section「子代理模型」（order 70，下拉取自 `llm-pi-ai` 同源清单，不可用退化为文本输入）；默认 = preset 固定路由 `adam/deepseek-v4.1-flash`，清空即回退；保存走原子写 settings.yaml。 |
| **图像能力检测（直传 vs 转文本）** | 已实现·**实测生效**（2026-09-16 实现；2026-09-17 声明并实测直传） | [vision-settings-capability-exec.md](.workspace/reports/execs/vision/vision-settings-capability-exec.md) §2/§3 · [mmt-probe/RESULTS.md](.workspace/probes/mmt/RESULTS.md) | 模型条目声明 `input` 含 `image` → 主会话/btw 原图直传，否则 vision-adam 转文本（检测基准 = 声明，非运行时探测）。2026-09-17 实测 adam 网关 `deepseek-v4.1-flash` 原生多模态**可用**（UI 图大字/色值逐字命中 15/17）→ settings.yaml 已为 `deepseek-v4.1-flash`、`deepseek-v4-flash-vision-exp` 声明 `input: [text, image]`，主会话带截图不再经识图插件；`deepseek-v4-flash` **故意不声明**（实测看不懂图、静默空输出）。 |
| **dsh-pptmaster（skill + 插件）** | 已实现·实测（2026-09-14 部署） | [deploy-pptmaster/04-Runbook.md](.workspace/workstreams/deploy/deploy-pptmaster/04-Runbook.md) · [pptmaster-exec.md](.workspace/reports/execs/pptmaster/pptmaster-exec.md) | PPT 生成/编辑：skill `ppt-master`（pn1024/dsh-ppt-master v6.1.0，attribution_guard 通过）+ 插件 `@local/dsh-pptmaster`（dsh-workbuddy-ppt 0.1.0 改名，工具 `pptmaster_*`）；工作区 .pptx 落盘约定见 03-integration.md。 |
| **dsh-workerspace 本地串口/烧录** | 已实现·**未真机**（2026-09-14；42 单测本地全绿） | [deploy-workerspace/RUNBOOK.md](.workspace/workstreams/deploy/deploy-workerspace/RUNBOOK.md) · [workerspace-exec.md](.workspace/reports/execs/workerspace/workerspace-exec.md) | `ws_serial_list/open/send/read/close`（stty 默认后端 + serialport 可选）+ `ws_flash`（白名单模板 esptool/openocd/dfu-util/uuu/fastboot + 高危确认模态 + 输出脱敏）；**真机串口探测/烧录未实测**，装后必须真 boot 冒烟（静态兼容 ≠ 能启动）。 |
| **分布式控制（dsh-ssh-gui v0.2.0）** | 已实现·**未真机**（2026-09-15；81/81 单测本地通过） | [deploy-ssh-gui/RUNBOOK.md](.workspace/workstreams/deploy/deploy-ssh-gui/RUNBOOK.md) · [distributed-control-exec.md](.workspace/reports/execs/distributed/distributed-control-exec.md) | 统一「分布式控制节点」：SSH / 本地串口 / TCP 串口三类传输，`~/.dsh/remote-workspaces/nodes.json`（0600）统一注册表（machines.json 首启迁移 + 双向同步）；侧栏「分布式节点」树、settings 三类 CRUD、header「节点」命令面板/串口控制台（不内嵌 PTY）；keyRef 只存引用名；exec 审计恒开。**真机 SSH/串口/serial-tcp 服务器未实测**（需部署后按 RUNBOOK §5）。 |
| **dsh-taste / dsh-wallpaper-local / session-board** | 已实现·实测（基线启用） | [master-runbook.md](.workspace/reports/runbooks/master-runbook.md) §0 · [docs/runbooks/port-taste.md](docs/runbooks/port-taste.md) · [docs/runbooks/port-wallpaper.md](docs/runbooks/port-wallpaper.md) | 偏好记忆 / 静态壁纸本地 fork / 会话状态看板：简单启用状态；0.1.5 bridge/命名空间移植记录见各 port 报告。 |

## 二、补丁（全局树官方包改动，重放脚本管理；重装全局树后需重放）

| 批次 | 状态 | 入口 | 一句话说明 |
| --- | --- | --- | --- |
| **lag-fix 5 补丁 + settings**（②b 子代理非流式 / mux 订阅过滤 seam / FrameQueue 有界 4096（应答帧永不丢弃）/ 图片变换 waterfall+门禁放行 / materialize 冷恢复 / tok·s 显示 / x-opencode-session 头；settings：adam maxTokens 990000、opencode-go 追加 deepseek-v4.1-flash、vision-adam 段） | 已实现·实测（2026-09-12 部署；六项启动问题 09-14 实测全部解决） | [deploy-lag/replay-lag-fix.sh](.workspace/workstreams/deploy/deploy-lag/replay-lag-fix.sh) · [lag-fix-runbook.md](.workspace/reports/runbooks/lag-fix-runbook.md) · [master-runbook.md](.workspace/reports/runbooks/master-runbook.md) | 一键恢复 4 包 + dsh-subagent + settings（备份/应用/校验/回滚/幂等）；覆盖 5 个既有补丁包。 |
| **0.1.5 借码批次（patch-official-015.sh）**：lean 12（P0：tool-web untrusted-notice、atomic-write Windows rename 重试（Linux no-op）；P1：goal attempt-attribution、user-approval scopeTarget 路由、mcp-client cursor 去重；P2：fs byte-range、bash 状态报告、str-replace null 占位、launchedThroughSsh、image-tokens 等）+ 组A 6（spill 清理 sweep、saveTextFile 竞态重试、ConnectionController 恢复增强、trajectory zh 字典…）+ 组B 4（subagent 族：effort 泄漏修复、run-settlement 诊断、fork/spawn agentOptions 旗标、tool-subagent 模型选择）+ 组C 8（session tool/result isError、projection restore 校验、reasoningEffort、WS 心跳+写串行化、session-persistence 错误、llm-retry 投影化、模型切换 notice） | 已实现·实测（2026-09-15 三段式重放完成；终审 32 包 225 .js node --check 全绿） | [deploy-lag/patch-official-015.sh](.workspace/workstreams/deploy/deploy-lag/patch-official-015.sh) · [deploy-015/patches/](.workspace/workstreams/deploy/deploy-015/patches) · [final-audit-a-patches.md](.workspace/reports/audits/final/final-audit-a-patches.md) | 0.1.5 高价值项向 0.1.1 最小面移植（unified diff + 完整副本 + sha256 锚点 + 幂等）；image-tokens 为纯新增导出（无消费者）；launchedThroughSsh 纯导出新增。 |
| **goal 修复（P0-A + 方案 A）** | 已实现·**已活体验收**（2026-09-17；首次真实观测：等待期零 goal 轮注入、唤醒源为 `subagent-settled` 通知；独立复核 271 行报告无未封堵路径） | [acceptance-exec.md](.workspace/reports/execs/acceptance/acceptance-exec.md) §1 P0-4 · [verify-goal-gate.md](.workspace/probes/acceptance/verify-goal-gate.md) · [goal-p0a-exec.md](.workspace/reports/execs/goal/goal-p0a-exec.md) | dsh-goal-round-driver：宿主 pause 中止运行中 round（keepInbox 不丢状态）+ 等待 subagent 期间空转注入修复（per-agent `pendingSubagents` 计数 + `competingQueued` 窄窗口兜底）。**R1 可见性加固（2026-09-17，仅日志、零行为变更）**：两处归因失败分支加 `ctx.logger.warn`，把唯一 fail-open 方向从静默变为可观测（sha `c4f3ea68…` → `4351e1742d3a…`）——**宿主 lib 属冷面，该加固待下一次重启才生效**。 |
| **subagent 模型 settings 默认层（dsh-tool-subagent P0' 补丁）** | 已实现·实测（2026-09-17；端到端双向真实派发：覆写→子代理实际跑 `glm-5.3`，清空→回退 `deepseek-v4.1-flash`） | [subagent-model-settings-exec.md](.workspace/reports/execs/subagent-model/subagent-model-settings-exec.md) · [deploy-subagent-model/dsh-tool-subagent.p0.diff](.workspace/workstreams/deploy/deploy-subagent-model/dsh-tool-subagent.p0.diff) | 官方包 `dsh-tool-subagent` execute 在 `requestedAgentOptions` 合并前插入 settings 默认层：读 `dsh-subagent` 命名空间 provider/model（**settings 优先、preset agentOptions 兜底**；读失败降级静态路由不报错）；层序 = 工具显式选择(S21 休眠) > settings > preset > 父路由继承；非法值经 preflight 实时校验抛错。**宿主 lib 改动需重启一次生效**（已于 2026-09-17 17:45:39 重启加载；实测重启后所有子代理 `agentModel=deepseek-v4.1-flash`）。**S21 定性（2026-09-17 裁决：否决启用）**：`modelSelectionSettings: true` 会在装配期 `throw` —— 该开关要求的宿主半边模块（`dsh-tool-subagent` 的 `./model-selection-settings` export）在本版本树（0.1.1-rc.2）**不存在**（仅 0.1.5 归档树有）；且其语义（模型自选 provider/model）与「路由调整须用户明确指令」冲突。详见 [acceptance-exec.md](.workspace/reports/execs/acceptance/acceptance-exec.md) §3.4。 |
| **槽位 B（sidebar.workspaces.remoteHosts）** | 已实现·实测（2026-09-15；48/48 单测） | [deploy-slots/patch-official-slots.sh](.workspace/workstreams/deploy/deploy-slots/patch-official-slots.sh) · [deploy-slots/REPLAY.md](.workspace/workstreams/deploy/deploy-slots/REPLAY.md) | 官方 ui-workspace 补丁（client.js +4 行、slots.d.ts +9 行）新增 sidecar list 槽 + ssh-gui 侧栏「分布式节点」树（复用「打开为工作区」链路）；空槽零 UI。 |
| **btw P0 materialize（dsh-subagent 官方补丁）** | 已实现·实测（2026-09-12） | [deploy-p0/APPLY-P0.md](.workspace/workstreams/deploy/deploy-p0/APPLY-P0.md) | `materializeContinuableChild`：仅恢复不投递的冷恢复公开方法；btw 打开冷子代理会话（host 打开链路不要求 parent live）。 |
| **未采纳 / 勿借（如实列出）**：P0-B `startsRequestSeries`；流式/传输层整族（api-gateway、remote.mux WS 重写、client-connection、http-proxy 架构级变更）；`CallId→ToolCallId` 重命名；S15（workspace 搜索 reveal 滚动）/ S16（空白行不计折叠上限） | **延期 / 勿借**（2026-09-15 裁决） | [upstream-015-diff.md](.workspace/reports/audits/upstream/upstream-015-diff.md) §4 · [borrow-015-exec.md](.workspace/reports/execs/borrow-015/borrow-015-exec.md) §6 | 架构级破坏面或超出最小面，未移植；S15/S16 为 P2 workspace UI 项，顺延（当前不可用）；借码时**不要**围绕这些设计。 |

## 二·附、2026-09-17 重启后验收批次修复（验收产物，详见 acceptance-exec.md）

| 项 | 状态 | 入口 | 一句话说明 |
| --- | --- | --- | --- |
| **context overflow 根因修复（adam 45 条 `contextWindow` 补齐）** | 已实现·**实测止血**（2026-09-17 18:04 热载，无需重启） | [acceptance-exec.md](.workspace/reports/execs/acceptance/acceptance-exec.md) §3.6 · [rootcause-context-overflow.md](.workspace/probes/acceptance/rootcause-context-overflow.md) · [context-window-table.md](.workspace/probes/acceptance/context-window-table.md) | 根因：adam 50 个模型条目里 45 条未声明 `contextWindow` → 回落 `dsh-llm-pi-ai` 默认 **262144**，pi-ai 客户端预估把正常轮次末步误判 `CONTEXT_WINDOW_EXCEEDED`（`maxTokens` **不参与**溢出判定）。修复：按「证据优先保守档」补齐 40 条（E1 同文件条目 7 / E2-cat 本机 pi-ai 目录规格 28 / E3 兜底 1000000 5），5 条非对话模型（veo ×2 + gemini 图像变体 ×3）不填。实测：`request/context` 事件 17:46 `262144` → 18:04 `1000000`（同进程热载）；受压探测把 `deepseek-v4.1-flash` 真实窗口夹到 **(998,887, 1,055,800]**，与 2^20=1,048,576 吻合。**注意**：该网关对超窗请求返回的是 `500 get_channel_failed`（非长度错误文案），所以客户端窗口声明必须准。 |
| **vision-adam URL 拼接加固** | 已实现·实测（2026-09-17） | [acceptance-exec.md](.workspace/reports/execs/acceptance/acceptance-exec.md) §3.7 · [fact-baseurl-slash.md](.workspace/probes/acceptance/fact-baseurl-slash.md) | 手写 `` fetch(`${options.baseURL}/chat/completions`) `` 加 `.replace(/\/+$/, "")` 归一化（部署位与源码副本逐字节一致，`node --check` 通过）。这是真正跑出过 `Invalid URL (POST /v1//chat/completions)` 的路径（失败工具 `analyze_image`；该文案实为网关 404 响应体）。 |
| **provider adam baseURL 去尾斜杠** | 已实现（2026-09-17） | 同上 | provider 层尾斜杠实测无害（OpenAI SDK `openai/client.js:224` 会切路径前导斜杠），去掉它是为了不再单一依赖 SDK 消重。 |
| **btw vitest 时序断言加固** | 已实现·实测（单文件 18/18、全量 231 passed / 2 skipped） | `dsh-btw/tests/host-opening.spec.ts:145` | 原断言在固定 100 个 `setImmediate` tick 内轮询一次含 fs 往返的 admission → 负载下会假失败（2026-09-17 17:54 并发时实测 1 次；隔离 10/10、全量复跑 3/3 均通过）。改为 wall-clock 10s 条件等待，语义不变。 |
| **仓库瘦身 A 路线（停止跟踪 `.venv-ppt-test`）** | 已实现（2026-09-17） | [acceptance-exec.md](.workspace/reports/execs/acceptance/acceptance-exec.md) §3.3 · [brief-history-bigfile.md](.workspace/probes/acceptance/brief-history-bigfile.md) | HEAD 曾跟踪 `.workspace/workstreams/deploy/deploy-pptmaster/.venv-ppt-test/` = 1817 文件 / 51.62 MiB（占 HEAD 跟踪内容 52.8%，未 ignore），由 `3028e624` 误入库 → `git rm -r --cached` + `.gitignore`。历史不动（mgr.tgz 51.08 MiB 仍在历史，但「GitHub 持续告警」已证伪 = 09-15 单次 push 的一次性 GH001）。 |
| **btw 图像管线 D1/D2 修复** | 已实现·**待重启生效**（2026-09-18；类型检查通过、vitest 233 passed / 2 skipped，新增两条针对性测试） | [btw-image-pipeline-audit.md](.workspace/reports/audits/btw/btw-image-pipeline-audit.md) · [usage-chart-restart-acceptance.md](.workspace/reports/execs/acceptance/usage-chart-restart-acceptance.md) §4.3/§4.4 | **D1**：`imageRefsByMessageId` 原先只在活体 entry 收图时写入、resume 后拿到空 Map 且无重建路径 → 关抽屉再打开历史图缩略图消失、`readImage` 回 `Unknown attachment id`；修复为 resume 后从子会话日志的 `image` 内容块回填（`hydrateImageRefs`）。**D2**：侧聊工具白名单有 `read_image` 无 `analyze_image` → 补上（仍与父工具注册表取交集）。**诚实边界**：用户报告的"截图找不到"经审计**无法复现**（找到 3 次成功直传），D1 是最可能的表象来源。**"btw 贴图不产生 `request-images/` 派生文件"已结案（2026-09-18）**：不是管线差异，而是**请求预算**——pi-ai 路由 `maxBytes=1 MiB` / `maxPixels=2048²`，图已在预算内则原样直通、**只有需要重编码时才落盘**（`createRequestImage:634` + `readRequestImageFile:723`）；用户那张 5K 截图入库后仍 1.68 MiB 超预算→必有派生，btw 的 522 KB 图在预算内→无派生，主代理自己 `read_image` 看的 5 张同样无派生（行为级反证）。详见 [btw-request-images-explained.md](.workspace/reports/reference/btw-request-images-explained.md)。宿主 lib 属冷面 → 需重启一次。 |



## 三、热载能力（2026-09-16 实测批次）

| 能力 | 状态 | 入口 | 一句话说明 |
| --- | --- | --- | --- |
| **patch 条目级热载** | 已实现·实测（2026-09-16，运行实例实测） | [deploy-lag/README.md](.workspace/workstreams/deploy/deploy-lag/README.md) §9 · [p0a-patch-hmr-exec.md](.workspace/reports/execs/p0-hotload/p0a-patch-hmr-exec.md) | `cordis.patch.yml` 的 insert（bare 包名）/remove/disable/name 更换/config 覆盖均约 1s 热生效，全程免重启；**硬约束**：insert 用文件路径名在运行实例 import 不执行（整次回滚），改插件宿主代码不热。 |
| **settings 值级热载（行为开关）** | 已实现·实测（2026-09-16） | [p0b-settings-switch-exec.md](.workspace/reports/execs/p0-hotload/p0b-settings-switch-exec.md) §4 | 改 `~/.dsh/settings.yaml` → 解析 → deep-equal commit → 客户端镜像/宿主读取全部走新值，无需重启；schema 与代码改动需随插件部署重启一次。 |
| **subagent 模型值级热载** | 已实现·**已重启验收**（2026-09-17；重启后实测 `dsh-subagent` 段缺失时全部子代理取 preset 路由 `deepseek-v4.1-flash`；覆写分支沿用 :8091 测试实例 E2E） | [acceptance-exec.md](.workspace/reports/execs/acceptance/acceptance-exec.md) §1 P0-3 · [subagent-model-settings-exec.md](.workspace/reports/execs/subagent-model/subagent-model-settings-exec.md) §热载语义 | 改 `settings.yaml` 的 `dsh-subagent:` 段（或设置页保存）→ **无需重启**，下一次 subagent / subagent_fork 派发即用新模型；段缺失/清空 → 回退 preset 默认。**preset 文件生效面已源码纠正（2026-09-17）**：`mount()` 只在**会话创建**时调用，`ensureStanding` 内有 compositionStamp(mtimeMs+size) 重挂载检查 → **新建会话本来就能拿到新 preset**；不热的是"改动前已存在的会话及其子代理"（子代理走 `composeFrom` 复用父 standing mount）。所以「改 preset 后 41s 派发的子代理仍用旧模型」的根因是**父会话早于改动**，而非 preset 永不热。要热换子代理模型请改 settings 段（已裁决不做 preset 热化，收益≈0）。 |
| **client bundle 热载** | 已实现·实测（机制） | [usage-tooltip-exec.md](.workspace/reports/execs/usage/usage-tooltip-exec.md) §5 | 插件 `lib/client.js` 替换后**刷新浏览器即生效**，无需重启宿主（dsh-usage/btw 均为此形态）。 |
| **dsh-restart 自动重启** | 已实现·实测（2026-09-16；dry-run/防抖/daemon 生命周期全验证；真实重启由用户执行） | [deploy-lag/dsh-restart.sh](.workspace/workstreams/deploy/deploy-lag/dsh-restart.sh) · [p0c-restart-helper-exec.md](.workspace/reports/execs/p0-hotload/p0c-restart-helper-exec.md) | 一键优雅重启（SIGTERM 有界等待 dispose → 重启 → 冒烟 200，默认只读预览+确认）+ `--watch` 保存即自动重启（md5 防抖 + daemon 可 `--stop`）；会话自动 resume，进行中回合内存态丢失为已知取舍。 |
| **纯函数热载 B 通道** | **尚不可用**（闸门通过 · 未投产，2026-09-16） | [p1-hotswap-gate-exec.md](.workspace/reports/execs/p0-hotload/p1-hotswap-gate-exec.md) · [p1-hotswap-lab/](.workspace/probes/hotswap-lab) | 实验证明 CJS+ESM 混合纯函数模块热载机制可行（internal 真清 loadCache，可回滚、无泄漏），但投产需改官方 `cordis-plugin-hmr`（解除 node_modules 排除）+ 模块白名单 + 一次重启落地（鸡生蛋问题）——当前不可用，勿围绕它设计。 |

---

## 诚实边界（本页之外，还有哪些不可用 / 条件性）

- **真机串口与烧录未实测**：`ws_serial_*` / `ws_flash` 只过了本地单测与静态加载；真机探测、烧录、串口日志需按
  [deploy-workerspace/RUNBOOK.md](.workspace/workstreams/deploy/deploy-workerspace/RUNBOOK.md) §5 实测后才算「已实现·实测」。
- **分布式控制真机面未实测**：SSH 命令执行、真串口、serial-tcp 服务器、nodes.json 0600 落盘均需部署后按
  [deploy-ssh-gui/RUNBOOK.md](.workspace/workstreams/deploy/deploy-ssh-gui/RUNBOOK.md) 实测。
- **vision 原图直传已按声明生效**：检测基准 = 模型条目声明（settings.yaml `input` 字段）。2026-09-17 已为 adam
  `deepseek-v4.1-flash` / `deepseek-v4-flash-vision-exp` 声明 `input: [text, image]`（实测原生多模态可用）→ 主会话原图直传；
  未声明 `image` 的模型（如 `deepseek-v4-flash`）仍走 vision-adam 转文本，把图直发给未声明的模型会被宿主闸门拒绝（预期）。
- **直传的小字保真度有限**：实测小字（10-11px）在直传下会退化为语义替换式幻觉（放大裁剪后同一路径 3/3 正确）；
  用户裁决不做放大预处理，故小字场景仍以 vision-adam 路径对照为准。
- **B 通道热载未投产**（见上）；**S15/S16 workspace UI 顺延**；**P0-B startsRequestSeries 勿借**。
- 本仓库为个人定制部署，clone 到别处大概率不能开箱即用；能力状态随部署演进，以最新 exec 报告与 README 验收矩阵为准。

## 接下来去哪

| Tier | 问题 | 页面 |
| --- | --- | --- |
| **1 · 部署** | 怎么让这些能力上屏/生效？ | [master-runbook.md](.workspace/reports/runbooks/master-runbook.md) + 对应主题 Runbook（README 索引） |
| **2 · 能力** | 用哪个能力、怎么用？ | 本页 → 各「入口」列的文档/脚本 |
| **3 · 证据** | 为什么这么实现、有什么取舍？ | `.workspace/*-audit.md` / `*-exec.md`（每项入口列已给出） |

# dsh-hub 文档风格约定（DOC-STYLE）

> **Tier 0 · 约定** · [指南地图](README.md)

本文档定义 dsh-hub 仓库所有文档的写作约定（范本：Luxweft Effect Pack 作者指南，
见 `.workspace/workstreams/research/research-luxweft-doc/zh-cn/`）。**未来所有 dsh-hub 文档按此约定写。**
约定不覆盖历史文档（`audit-*.md` / `*.exec.md` 等证据类报告按落盘时的档期格式保留），
但新写的文档、README、功能地图与 Runbook 一律遵守。

## 1. 语言与受众

- 正文以中文为主；代码、命令、包名、符号、状态标记保留原文（如 `ws_flash`、`cordis.patch.yml`）。
- 术语首次出现给中文释义或对照（如「候选代事务 → candidate generation」）。

## 2. 归属表开头（谁拥有什么）

解释职责/边界/分工的第一节用「归属表」：

| 职责 | 归属 |
| --- | --- |
| … | … |

判断标准要**可复判**（Luxweft 01-overview 模式）：「如果移除 X 之后行为仍会被主动
改变，那这个行为属于 Y，放错了位置；如果两个选项可以合理地选择不同算法，它就属于
可配置方。」例：本仓库「补丁脚本管代码层、dsh-restart 管进程层」即用归属表/分工表开头。

## 3. 普适规则可复判

- 提炼「三条规则」这类心智模型时，每条规则必须能解释大部分行为，且附**来源/可复判**
  说明——来自哪份落盘报告、哪次实测（文件路径 + 日期）。
- 不写无法验证的断言；规则与实现不一致时，以运行实例的实际行为为准，并回报主代理修正文档。
- **fail-closed 是必写规则**（README 规则 3「应用失败即失败，绝不静默部分生效」），
  涉及自动化改动/高危操作的文档都要带上它，且用真实事实支撑：
  - 补丁脚本：每单元应用后必校验（node --check + 锚点 + sha256/字节比对），校验失败 → FAIL
    退出非零、绝不静默继续，`--rollback` 一键还原（来源：`.workspace/reports/execs/lagfix/lag-fix-exec.md` U-9）；
  - `cordis.patch.yml` 热载：刷新失败 → 整次回滚、无残留（来源：`.workspace/reports/execs/p0-hotload/p0a-patch-hmr-exec.md` §0/§1）；
  - `ws_flash`：高危烧录每次调用都须确认模态，未授权模板一律拒绝（fail-closed，
    来源：`.workspace/reports/execs/workerspace/workerspace-exec.md` §1-2 四道闸）；
  - 能力检测：模型能力**未知声明 → 保守回退 vision-adam**，绝不把图直发给文本模型
    （来源：`.workspace/reports/execs/vision/vision-settings-capability-exec.md` §2 回退安全）。

## 4. 代码块规范（demo 代码块）

文档里的命令块必须可复制、可运行、可验证：

- **完整可复制**：整段命令可从文档直接粘贴执行；需要多个步骤时给完整序列，不用省略号
  替代中间命令。
- **与真实命令逐字一致**：命令必须与真实脚本/Runbook 一字不差（可从
  `.workspace/workstreams/deploy/deploy-*/` 脚本复制），不允许示意性改写；环境相关部分用显式占位符
  （如 `<deploy>/dsh-workerspace`）并注明可替换。
- **带预期输出注释**：用 `# 预期输出：…` 注释标出关键成功行（如「全部单元 PASS」、
  「HTTP 200」、「graph 行出现」），读者能自判是否成功。
- **可运行验证**：文档中的命令必须在本仓库/本机实态下可执行（如 `--dry-run` 零副作用
  先行），严禁编造命令或输出。
- README 与 Runbook 的命令块一律按本规范（示例见 README「部署与验证 Runbook 索引」的
  补丁重放代码块）。

## 5. 导航表带「回答什么」列

页面/章节导航一律用表格，且带「回答什么」列：

| 页面 | 回答什么 |
| --- | --- |
| 一行相对路径链接（示例占位） | 一句话说明该页解决什么问题 |

路由表（「从这里开始」）用「你的情况 | 路径」两列，按处境而不是按目录组织。

## 6. 状态词汇（统一，带日期）

全仓库统一用以下五种状态标记（README 与功能地图已内置，其它文档直接引用即可）：

| 状态标记 | 含义 |
| --- | --- |
| **已实现·实测** | 已落地部署，并在本机实测/验收过（GUI 验收矩阵或真实调用）。 |
| **已实现·未真机** | 代码与本地单测通过，但依赖真机环境的部分（串口、烧录、远端主机）未实测。 |
| **尚不可用** | 机制存在或闸门通过，但当前不可用（须给具名原因）。 |
| **计划中** | 当前未实现，计划后续提供。 |
| **延期** | 长期方向；不要围绕它设计。 |

规则：
- 每个状态条目**必须带日期**（YYYY-MM-DD），例：`已实现·未真机（2026-09-14）`。
- 「已实现·未真机」必须注明**哪部分**未实测，不能笼统带过。

## 7. 诚实边界

- 每篇文档明确「本指南/本页不包含什么」（Luxweft：能力地图 *尚不可用* 一节，不当作
  可用功能写）。
- 未开放或条件性的能力（真机串口/烧录、B 通道热载、vision 直传需声明 input 等）如实
  标状态并给原因，不隐含可用。
- 个人定制边界：本仓库是作者本机部署的定制成果，不是通用发行版；clone 到别处大概率
  不能开箱即用——README 顶部必须保留该警示。

## 8. 面包屑 / Tier 标注

- 每页顶部一行：`> **Tier N · 主题** · [指南地图](README.md)`。
- Tier 划分（dsh-hub）：
  - **Tier 0 · 约定与范式**：README（导航中枢）、DOC-STYLE（本文）、方法论（`~/.dsh/AGENTS.md`，不入库）
  - **Tier 1 · 部署与验证**：`master-runbook.md`、各主题 Runbook（deploy-*/RUNBOOK、APPLY、REPLAY）、`examples/minimal-plugin/`（脚手架）
  - **Tier 2 · 能力**：FEATURE-MAP.md（每项能力的状态与起点）
  - **Tier 3 · 参考与证据**：`.workspace/*-audit.md`、`*-exec.md`（审计结论、交付单元、自复核证据）、`.workspace/workstreams/deploy/deploy-*/patches/*.patch`（可执行规范）

## 9. 表格扫读优先

- 状态表、路由表、导航表优先用表格；每格短句，「一句话说明」控制在单行内。
- 文档以「结论式摘要」开头（TL;DR），再展开细节；长报告按 §编号分层。

## 10. 来源与方法论（写作纪律）

- 事实必须来自落盘报告（`.workspace/*-audit.md` / `*-exec.md` / `*-runbook.md`），**不臆造**；
  引用时给文件路径，结论与证据分离（结论在文档，证据在报告）。
- **实现参考即可执行规范**：`.workspace/` 各 exec 报告与 `deploy-*/patches/*.patch` 是本仓库
  的「参考 Pack」等价物——patch 文件可直接 `patch -p1` 在包目录应用（README「实现参考」节），
  文档引用它们时给路径与一句话语义，不复制大段内容。
- 两阶段闭环（审计 → 修订执行复核一体）是全部改动的生产方式；文档记录裁决与取舍，
  不替主代理做设计决策。
- 歧义/越界如实上报主代理，不自行拍板；本约定自身有歧义时以 Luxweft 范本
  （`.workspace/workstreams/research/research-luxweft-doc/zh-cn/`）为准。
